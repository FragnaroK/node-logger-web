{
  "name": "node-logger-web",
  "version": "1.0.7",
  "description": "Web based node logger",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Warning: No tests implemented\" && exit 0", 
    "build": "tsc-builder prod",
    "deploy": "npm run build && npm publish"
  },
  "author": "FragnaroK",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "files": [
    "build/**/*"
  ],
  "devDependencies": {
    "@fragnarok/tsc-builder": "^1.0.5"
  }
}
